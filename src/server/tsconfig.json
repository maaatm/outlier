{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
    /* No `customConditions` here on purpose — the server wants the default
     * (Node) entrypoints of `@devvit/web/*`. */
  },
  "include": [".", "../shared", "../../data", "../../tests", "../../*.config.ts"]
}
